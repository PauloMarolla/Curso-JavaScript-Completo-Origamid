const h1Selecionado = document.querySelector('h1');

h1Selecionado.style.color = "blue";

// titulo.innertext = retorna o texto;
// classlist = retorna as classses;
// id = retorna id
// offSetHeight = retorna a altura do elemento

// addEventListener

console.log(window.location.href);

const h1Active = document.querySelector('.ativo');
console.log(h1Active);

const pagComplete = document.documentElement.lang;

const navegatorLang = window.navigator.language;

console.log(pagComplete);
console.log(navegatorLang);


const larguraTela = window.innerWidth;

console.log(larguraTela);